# 可选。不配也能跑：模拟面试用本地题库，面经抽取走规则解析。
# 字幕翻译需要模型 —— 访客可以在页面上填自己的 key，无需服务端配置。
# 这里配了，才会给没有自带 key 的访客提供免费试用额度（见文件末尾）。
OPENAI_API_KEY=

# 服务端 OpenAI 调用使用的模型（模拟面试出题与点评、面经抽取）。
# 这些都是判断类任务，中档模型合适；旗舰档在这里没有对应收益。
# 浏览器端的翻译/分析模型由访客在页面上各自配置，与这里无关。
OPENAI_MODEL=gpt-5.6-terra

# 字幕翻译单独用便宜模型。翻译调用量最大且是机械转换，跟上面那个判断类模型
# 共用会贵二十倍。
OPENAI_TRANSLATE_MODEL=gpt-4o-mini

# Absolute site URL, used for canonical metadata, sitemap.xml and robots.txt.
NEXT_PUBLIC_SITE_URL=

# Comma-separated keys for the public /api/v1 surface. Leave empty and those
# endpoints stay open — set it before exposing the site to the internet.
# Generate one with: openssl rand -hex 24
API_KEYS=

# 字幕服务（必填）。这是唯一的字幕来源 —— 不配就读不到任何字幕。
# 免费档 100 次/月，不需要绑卡：https://dash.supadata.ai/
SUPADATA_API_KEY=

# --- 免费试用额度 -----------------------------------------------------------
# 配置 OPENAI_API_KEY 后，没有自带 key 的访客可以直接试用，费用由你承担。
# 三重上限封死支出：单 IP 每天次数、全站每天总次数、单次翻译条数。
# 按 gpt-4o-mini 计价，单次约 $0.003，默认配置每天最多约 $0.30。
# 设 TRIAL_TOTAL_DAILY=0 可完全关闭试用。
# TRIAL_PER_IP_DAILY=3
# TRIAL_TOTAL_DAILY=100
# TRIAL_MAX_SEGMENTS=150

# --- Optional tuning -------------------------------------------------------
# Per-call OpenAI budget in ms (default 30000).
# OPENAI_TIMEOUT_MS=30000
# 单个视频最多翻译多少条字幕（默认 3000）。
# 一部 2~3 小时的访谈大约 1500~2500 条，调低会从中间截断。
# MAX_CAPTION_SEGMENTS=3000
# Rate limit per client IP (defaults: 10 requests / 60000 ms).
# RATE_LIMIT_MAX_HITS=10
# RATE_LIMIT_WINDOW_MS=60000
# Set to "memory" on serverless platforms where the filesystem is read-only.
# CACHE_BACKEND=memory

# --- MCP server (mcp/server.mjs, runs on your machine not the server) -------
# SANCIA_API_BASE_URL=https://your-domain.com
# SANCIA_API_KEY=one-of-the-API_KEYS-values
# SANCIA_API_TIMEOUT_MS=120000
