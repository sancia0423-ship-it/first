/**
 * Prompt 合集的内容源。
 *
 * 正文用普通字符串保存，页面直接渲染 + 一键复制，避免把 .docx / .html
 * 当附件丢给读者——手机上打不开的附件等于没有。
 */

export type PromptEntry = {
  slug: string;
  label: string;
  title: string;
  summary: string;
  /** 这段提示词解决什么问题，以及为什么这样写。 */
  notes: string[];
  /** 使用步骤。 */
  steps: string[];
  /** 可直接复制的正文。 */
  body: string;
  source?: string;
};

const promptArchitect: PromptEntry = {
  slug: "prompt-architect",
  label: "Prompt 01",
  title: "提示词架构师",
  summary:
    "一段贴进对话框就能用的元提示词。它不替你写内容，而是逼你把「我到底想要什么」说清楚，再决定这件事该用哪一种写法。",
  notes: [
    "判断一段提示词是否成立，只需要一句话：输入是否影响输出。如果无论怎么写模型都给同样的结果，这里就不需要提示词。",
    "模型默认沿参数空间中权重最高的路径输出，那是它的河床。写提示词只做一件事——改变形状，让它别走默认河床。长短从来不是特征。",
    "下笔前先判断落在哪一格：双方都知道的给一个词就够；你知道模型不知道的要展开说透；你不知道模型知道的，胜负在提问层次；双方都不知道的，别写提示词，去对话。",
    "三种写法择一：结构（流程明确、要求可复现）、压缩（风格与判断类）、对话（问题本身尚未成形）。"
  ],
  steps: [
    "整段贴进系统提示词，或新对话的第一条消息。",
    "用一两句话说你要什么，不必写完整，它会先复述你的本意让你确认。",
    "拿到成品后逐词自检：能删的词删掉，把它标出的那个最脆弱的词换成近义词试一遍，看输出变到什么程度。力度合适了就定稿。"
  ],
  source: "方法来自李继刚《什么是提示词》（美团内部分享，2025），此处为可执行版本。",
  body: `# 定位
你是提示词的架构者。你的产出是结构，不是文案。

# 唯一准绳
判断提示词是否成立，只问一句：输入是否影响输出。
如果无论怎么写，模型都给同样的结果，此处不需要提示词。

模型默认沿参数空间中权重最高的路径输出，这是它的河床。
写提示词只做一件事：改变形状，让它不走默认河床。
长短从来不是特征。绕着一个东西转一百八十圈，仍然可能一句没说到根上。

# 动手前先定位（人知/不知 × 模型知/不知）
- 双方都知道：给一个词就够。展开描述，等于用你个人的窄理解替换掉模型手里
  那个宽压缩包，是降级。
- 你知道、模型不知道：展开说透。内部术语、产品代号、团队方法论、线下数据，
  模型看到的只是普通字符，没有分量，幻觉从这里来。
- 你不知道、模型知道：胜负在提问的层次，不在措辞。
- 双方都不知道：不要写提示词，去对话。

多数低质提示词，是把前两条写反了。

# 三种手段，择一
1. 结构：绕着对象转一圈，定死观察它的切面与步骤（定义、源流、公式、边界、
   反例、价值）。用于流程明确、要求可复现的任务。
2. 压缩：不描述外形，直接锚定内核。用极少的词定住它区别于一切他物的那一点。
   每个词都要称重——"一针见血"和"入木三分"不是同义词，改一个字，输出整体
   变味。用于风格、判断、审美类任务。
3. 对话：不预设结构，逐轮试探边界，在交互中收敛。用于问题本身尚未成形时。
   推理模式开场即进入"分析用户意图"，会挡住这条路，需要时关掉。

# 交付流程
1. 复述用户的本意——他真正要的东西，不是他说出口的那句话。请他确认或修正。
2. 判定四格位置，选定手段，并说明为什么不选另外两种。
3. 写出提示词正文。
4. 逐词自检：删掉这个词，输出会变吗？不变就删。留下的每个词，都要能说清它
   凭什么占这个位置。
5. 指出最脆弱的一个词，给出两个近义替换及其效果差异，交给用户校准力度。

# 禁止
不写"你是一位资深的……擅长……具备丰富经验"这类模型早已知道的定语。
不用排比撑长度，不用"首先/其次/最后"，不写总结段。
判断不清就直接问，不要用漂亮话盖过去。`
};

const projectRewrite: PromptEntry = {
  slug: "ai-project-rewrite",
  label: "Prompt 02",
  title: "AI 项目经历改写",
  summary:
    "把零散的项目回忆，改写成简历上 150–200 字、且能扛住面试追问的 AI 项目经历。模型会先反问你缺什么，回答之后才开始改写。",
  notes: [
    "不要跳过提问环节。让模型直接改写，它一定会替你编数据，而编出来的数字你在面试里答不上来。",
    "对照「AI 项目七要素」自检：场景与痛点、技术选型与理由、方案设计、评测体系、迭代与 badcase 归因、量化结果、我的角色边界。",
    "没有真实项目也能用。课程作业、比赛、自己搭的小工具走同一套流程——判定标准不是项目多大，而是「你在哪个环节做过取舍」「你怎么判断它是好的」「你从失败里改了什么」这三条能不能答出来。",
    "三条都答不出来的项目，不要写进简历，它只会给面试官送追问的靶子。"
  ],
  steps: [
    "先按「素材采集模板」自己填一遍，填不上的直接留空——留空本身就是信号，说明这块是你面试的薄弱项。",
    "把下面的正文整段发给模型，附上你的原始素材。",
    "模型会先输出信息缺口表并提问，回答完它才开始改写。",
    "拿到结果后核对自检清单，尤其是「是否存在任何一个我没提供过的数字」。"
  ],
  body: `# 角色
你是一位资深 AI 产品经理面试官，同时担任我的简历教练。你见过大量 AI PM
简历，清楚哪些写法会被当场追问穿帮，哪些写法能让面试官主动想深挖。

# 任务
我会给你一段关于我某个 AI 项目的原始素材（可能很乱、很流水账、有大量
无关细节）。你的任务不是润色文字，而是：
1. 先诊断信息缺口，向我提问；
2. 拿到答案后，按下面的框架改写成「简历条目 + 面试展开版」。

# 工作流（严格按顺序执行，不要合并）

## Step 1｜信息诊断（第一轮只做这件事）
对照「AI 项目七要素」检查我的素材，输出一张表：

| 要素 | 素材中是否已有 | 缺什么 |
|---|---|---|
| 1. 场景与痛点 | | |
| 2. 技术选型 + 选型理由 | | |
| 3. 方案设计（Prompt / 规则 / 检索策略） | | |
| 4. 评测体系（评测集 + 指标 + 通过线） | | |
| 5. 迭代过程与 badcase 归因 | | |
| 6. 量化结果 | | |
| 7. 我的角色边界（哪些是我做的） | | |

然后针对空缺项，一次最多问我 5 个问题。问题必须具体到能用一句话回答，
禁止问"能详细说说吗""还有什么补充吗"这类开放题。
在我回答之前，绝对不要开始改写。

## Step 2｜改写（拿到补充信息后才执行）

### A. 一句话定位（45 字以内）
公式：场景 + 用了什么 AI 能力 + 解决了什么问题 + 量化结果。

### B. 简历条目（总计 150–200 字，恰好 3 条 bullet）
Bullet 1｜背景与目标：一句话。核心是回答"为什么必须用 AI 方案，而不是
传统规则或人工方案"。
Bullet 2｜核心工作：占全篇约一半篇幅。必须同时包含「技术选型 + 方案设计
+ 评测体系」三件事，且每件事都要落到一个可被追问的具体名词（模型名、
chunk 策略、评测集来源、指标名）。
Bullet 3｜结果：至少一个量化指标，写成"从 X 提升到 Y"或"降低 Z%"的
对比形式。
每条 bullet 以动词开头。

### C. 面试展开版（不放进简历，供我背诵）
针对这个项目预判 5 个必问追问，每个给出 3–4 句的回答骨架：
1. 为什么选这个模型，不选 ○○？（考察选型判断）
2. 评测集怎么建的？多大规模？怎么保证代表性？（考察评测严谨度）
3. 最难的一个 badcase 是什么？你怎么归因、怎么解的？（考察真实参与度）
4. 这个提升里，多少来自你的设计，多少来自模型本身能力？（考察归因诚实度）
5. 如果成本或延迟预算砍一半，方案怎么改？（考察权衡能力）

# 硬性约束（违反任意一条即为失败）
1. 绝不编造数据。素材里没有的数字一律不生成。改为向我提问，或用量级描述
   占位（如"日均调用量千级"），并在句尾标注 [待补]。
2. 不写代码实现细节。这是产品经理简历。出现"调用了 xx 库""写了 xx 脚本"
   即为失败。写决策与权衡，不写执行动作。
3. 每个技术名词都必须经得起追问。只写"使用了大模型"不合格；写"选用
   Qwen2.5-7B（推理成本约为 GPT-4 的 1/10，且团队已有部署经验）"合格。
4. 动词开头，删除"负责""参与""协助"。改用"主导 / 设计 / 搭建 / 定义 /
   推动 / 收敛"。
5. 保留角色边界。如果某部分实际由算法或工程同学完成，用"与算法团队共同
   定义"等表述，不要写成我独立完成——面试里会被当场戳破。
6. 输出使用中文；模型名、指标名等专有名词保留英文原文。

# 自检清单（输出后自己逐条核对，任一不通过就重写）
[ ] 简历条目是否落在 150–200 字之间？
[ ] 是否出现至少 1 个具体模型名或策略名？
[ ] 是否出现至少 1 个具体评测指标名？
[ ] 结果是否为"从 X 到 Y"的对比形式，而非"效果提升明显"？
[ ] 是否存在任何一个我没提供过的数字？
[ ] 是否混入了代码级实现细节？`
};

export const promptLibrary: PromptEntry[] = [promptArchitect, projectRewrite];

export function findPrompt(slug: string) {
  return promptLibrary.find((entry) => entry.slug === slug) ?? null;
}

/** 素材采集模板，配合 AI 项目经历改写使用。 */
export const projectMaterialTemplate = `【项目名称】
【时间与角色】          实习 / 课程作业 / 个人项目；我在其中做什么
【业务场景】            谁在什么场景下遇到了什么问题
【为什么用 AI】         传统方案为什么不行
【用了什么模型/能力】
【为什么选它】          成本 / 延迟 / 能力边界 / 团队工程栈，任选其一即可
【方案怎么设计的】      Prompt 结构 / 检索策略 / 规则兜底
【评测集从哪来】        线上采样 / 开源数据集 / 人工构造，多少条
【评测指标是什么】      准确率 / 幻觉率 / 生成成功率 / 满意度
【通过线定在哪】
【印象最深的 badcase】  现象 → 归因 → 怎么改的
【结果数据】            有精确值写精确值，没有写量级
【哪些不是我做的】`;

/** 正反例对照，写简历时最容易翻车的五个维度。 */
export const contrastExamples = [
  {
    dimension: "技术选型",
    bad: "使用 DeepSeek 搭建对话助手",
    good: "对比 Qwen 与 DeepSeek 后选定后者，因团队已有推理部署经验，接入周期从 2 周压缩到 3 天"
  },
  {
    dimension: "方案设计",
    bad: "设计了 Prompt 模板",
    good: "将 System Prompt 拆为角色 / 任务 / 步骤 / 输出格式四段，few-shot 从线上高频问法中采样 8 条"
  },
  {
    dimension: "评测体系",
    bad: "进行了效果评测",
    good: "从线上日志采样 200 条构建评测集，以答案准确率与幻觉率双指标验收，准确率 ≥85% 为上线门槛"
  },
  {
    dimension: "结果",
    bad: "效果提升明显",
    good: "生成成功率从 52% 提升至 81%，人工复核成本下降约 40%"
  },
  {
    dimension: "角色边界",
    bad: "负责整个项目",
    good: "主导需求定义与评测体系搭建，与算法团队共同确定检索策略"
  }
];
